#include<iostream>
using namespace std;
int main()
{
    double a, b;
    cin >> a >> b;
    /// scanf("%d%d", &a, &b);
    int sum = a + b;
    /// printf("The summation is: %d\n", sum);
    cout << "The summation is: " << sum << endl;
    return 0;
}

